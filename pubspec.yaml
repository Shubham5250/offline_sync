name: flutter_data_sync_manager
description: A Flutter package for seamless offline-first data synchronization between local and remote storage.
version: 1.1.2
homepage: https://github.com/Shubham5250/offline_sync
repository: https://github.com/Shubham5250/offline_sync
issue_tracker: https://github.com/Shubham5250/offline_sync/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0
  collection: ^1.19.1
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  test: ^1.24.0

topics:
  - offline
  - sync
  - database
  - storage
  - flutter

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: